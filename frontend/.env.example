# API base URL (only needed if not using the Vite proxy)
# In development, the proxy in vite.config.ts handles /api → http://localhost:3001
# In production builds, set this to your deployed API URL
VITE_API_BASE_URL=http://localhost:3001
